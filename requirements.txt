# --- core toolkit ---
httpx>=0.27
sentence-transformers>=2.2      # semantic gate (pulls in torch + transformers)

# --- target model + servers (target, shim, dashboard) ---
transformers>=4.40
torch>=2.2
fastapi>=0.110
uvicorn>=0.29

# Standard library used (no install): sqlite3, asyncio, difflib, dataclasses,
# hashlib, json, argparse, statistics, html, subprocess, unicodedata
